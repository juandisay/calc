# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['calc_gui.py'],
    pathex=[],
    binaries=[],
    datas=[('calc', 'calc_engine')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='calc',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['calc.icns'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='calc',
)
app = BUNDLE(
    coll,
    name='calc.app',
    icon='calc.icns',
    bundle_identifier='org.calc.gui',
)
